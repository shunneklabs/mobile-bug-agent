# E3 — Fallback demo video + runbook (#35)

Kickoff scaffold for Workstream E, Issue E3.

## Goal
- 90-second + 30-second silent fallback videos (1080p) of the full booth flow.
- Local copies stored under `docs/booth/` (or assets dir).
- Operator runbook for switching to fallback when wifi/Notion/GitHub fails.

## Acceptance criteria
Tracked in GitHub issue #35.

## Parent
- Parent PR: #37 (`workstream-e-parent` → `master`)
- Tracked issue: #35
